# pbgc-caseworkbench
Tools to help process terminated plan case work at PBGC

## V1 engine references

Approved V1 engines are user-selected read-only `V1Summary.json` reference inputs. They are not bundled into the single-file release; import them through the V1 Explorer workflow when needed.
