{
  "name": "web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "pack": "vite build && cd .. && node scripts/pack-single-html.mjs",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^7.3.1"
  },
  "dependencies": {
    "@xmldom/xmldom": "^0.8.11",
    "ajv": "^8.18.0",
    "jszip": "^3.10.1"
  }
}
